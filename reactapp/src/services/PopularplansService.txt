import axios from 'axios'
import authHeader from "./auth-header";

const PLAN_BASE_REST_API_URL = 'https://8080-ddbdacaccaaebbfaaecebafeebbfdeebfce.examlyiopb.examly.io/user';

class PopularplansService{

    getAllPlans(){
        return axios.get(PLAN_BASE_REST_API_URL+'/getAllPlan', { headers: authHeader() })
    }
    
    getPlanById(planId){
        return axios.get(PLAN_BASE_REST_API_URL + '/getPlan/' + planId, { headers: authHeader() });
    }

    createRecharge(plan){
        return axios.post(PLAN_BASE_REST_API_URL +'/addRecharge', plan, { headers: authHeader() })
    }

    getAllRecharge(){
        return axios.get(PLAN_BASE_REST_API_URL+'/getRecharge', { headers: authHeader() })
    }
}

export default new PopularplansService();